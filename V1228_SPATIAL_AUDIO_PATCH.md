# ZIVOZONE V1228 — HRTF Spatial Audio Integration

تم دمج نظام الصوت المحيطي المجسم داخل بنية V1228 بدون إزالة محرك الصوت القديم.

## ما تم تغييره
- `core/modules/spatial-rooms.js`: سجل مركزي للغرف + الأجواء + الثيمات + مواضع مصادر الصوت.
- `core/modules/audio-spatial.js`: محرك Web Audio API مستقل يستخدم `PannerNode` مع `HRTF`.
- `core/modules/zivo-platform-app.js`: ربط التنقل والـ hash والغرف والتحديات بالمشهد الصوتي والبصري.
- `core/modules/rooms.js`: منع ازدواجية ambient القديم عند توفر المحرك المجسم، وإرسال أحداث دخول/خروج الغرف.
- `zivo-ai.js`: ربط جلسة ZIVO AI بالغرفة النشطة وتشغيل حدث صوتي مكاني عند إرسال السؤال.
- `index.html`: تحميل طبقة spatial الجديدة وملف CSS الخاص بالانتقالات.

## سلوك التشغيل
- لا يتم إنشاء `AudioContext` عند تحميل الصفحة.
- يتم فتح السياق بعد أول تفاعل للمستخدم، احتراماً لسياسات autoplay.
- عند تغيير الغرفة يتم إيقاف الـ ambient القديم من المحرك التقليدي ثم تشغيل مصدر HRTF جديد.
- كل غرفة لها `distanceModel`, `maxDistance`, `rolloffFactor`, وموضع ثلاثي الأبعاد مستقل.
- المؤثرات السياقية والـ tones الخاصة بالتفاعل تمر عبر PannerNode.
- تغيير الغرفة يبدّل الثيم CSS ويضع `data-zivo-room` على `html/body`.
- يوجد انتقال بصري سريع أثناء تبديل المشهد.

## ملاحظة
HRTF هو spatial virtualization داخل Web Audio، والنتيجة تعتمد على المتصفح، السماعات/سماعة الرأس، وإعدادات النظام. لا يمكن ضمان قناة 5.1 فعلية من المتصفح لكل جهاز؛ HRTF هو المسار المناسب لصوت ثلاثي الأبعاد عبر سماعات الرأس/الستيريو.
