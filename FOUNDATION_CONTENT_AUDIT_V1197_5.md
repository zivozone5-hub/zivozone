# ZIVOZONE V1197.5 — FOUNDATION CONTENT / ROOM AUDIT

## الهدف

إغلاق أساس المحتوى والتحديات والغرف قبل البدء في تطوير كل غرفة بشكل مستقل.

## سياسة الإصدار

- لا يتم توليد أسئلة مستنسخة لبلوغ رقم 50.
- السؤال المكرر يُزال من نفس البنك مع الحفاظ على النسخة الأساسية.
- كل سؤال اختيار من متعدد يجب أن يملك `a[]` و `c` صالحين.
- كل سؤال إدخال يجب أن يملك `answer` أو `alternatives`.
- لا يسمح بتكرار خيارين متطابقين داخل السؤال.
- المحتوى الذاتي/التجريبي الذي لا يملك إجابة صحيحة واحدة لا يدخل في نظام التصحيح.

## النتيجة

- 21 بنكًا حيًا تم تدقيقها.
- 633 سؤالًا مؤلفًا فعليًا بعد إزالة التكرارات.
- 542 سؤال اختيار من متعدد.
- 91 سؤال إدخال/إجابة مباشرة.
- لا توجد أسئلة مكررة نصيًا داخل البنك بعد التنقية.
- لا توجد روابط إجابة مفقودة أو مؤشرات `c` غير صالحة.
- Dark Room subjective bank أصبح Non-Scored.

## Puzzle Room

تم إصلاح أساسيات محددة ظهرت أثناء التدقيق:

1. سؤال الرموز لم يعد يستخدم رموزًا متشابهة مثل `◆` و`◇` في نفس الجولة.
2. تم توضيح السؤال ليكون عن الشكل نفسه تمامًا.
3. لغز المنطق كان يسجل `رامي` رغم أن الحل المنطقي هو `ليان`؛ تم تصحيحه.
4. المتاهة القديمة لم يكن لها مسار مضمون إلى المخرج؛ تم استبدالها بخريطة ذات مسار صالح.
5. اتجاه الهدف في لعبة الدوران كان مخفيًا؛ أصبح الهدف ظاهرًا على كل قطعة.
6. مكافأة Puzzle Room لم تعد تُرسل مباشرة إلى Economy قبل التحقق من GamePlatform.
7. فشل التحقق يمنع إعلان `perfect` وبالتالي يمنع المكافأة.

## Dark Room

الأسئلة النفسية/الاختيارات الشخصية لا تُعامل كأسئلة صحيحة/خاطئة، لأن بعضها لا يملك إجابة موضوعية واحدة. النتيجة تصبح تجريبية/قصصية، دون مكافأة Perfect مبنية على اختيار شخصي.

## Forensic Lab

تم التحقق بنيويًا من:

- 25 قضية.
- 10 أسئلة لكل قضية.
- 10 حقائق لكل قضية.
- 5 مراحل لكل قضية.

ولا يتم تغيير `forensic-case-core.js` إلى محرك آخر.

## Game Registry

- IDs فريدة.
- الأصول المفعلة يجب أن تكون موجودة.
- SWF غير المفعّل لا يمنع الإقلاع.

## JavaScript

تم تمرير فحص Syntax على 49 ملف JavaScript.

## نتيجة بوابة الأساس

`11 PASS / 0 FAIL`

هذه البوابة تثبت سلامة **البنية والمطابقة والتوصيل الأساسي**، لكنها لا تعني أن كل سؤال تم تقييمه يدويًا بصريًا في المتصفح. اختبار Browser E2E الحقيقي يبقى بوابة منفصلة قبل Production Freeze.
