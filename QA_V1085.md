# ZIVOZONE V1085 QA — Static/Structural Check

- HTML static buttons/links inspected: 24
- Static data-action handlers found: ads-control, login, logout, open-identity, refresh-sports, scroll-challenges
- All static data-action names are present in the application bundle.
- Static hash navigation targets: OK (6 targets, 0 broken).
- JS syntax: `zivozone-app.js`, `zivo-ai.js`, `sw.js` — OK.
- External local CSS files referenced by HTML: 1 (`zivozone.css`).
- Non-module local application JS files referenced by HTML: 1 (`zivozone-app.js`).
- Firebase/Auth/Firestore script dependencies preserved.
- Existing challenge engine is preserved inside the unified bundle.
- Dark Room bank exists in the preserved runtime.
- Wallet/Mining core is preserved; economy mount moved from header to Home content.
- Player floating launcher removed; Profile area receives the same launcher.
- Password reset uses Firebase Auth `sendPasswordResetEmail` when cloud auth is active.
- Unreferenced `horror_scream.mp3` and `horror_laugh.mp3` were removed; Dark Room sound effects are generated in-browser by the existing runtime.
- Legacy source files are not shipped; original V1081 ZIP is the rollback copy.

## Important
This is a structural/static QA pass, not a claim of successful live Firebase transactions. Final live verification should include login, registration, password reset, wallet read/write, mining, a 10-question challenge, Dark Room, and news.
