# ZIVOZONE V1081 — Player / Wallet

Based on V1080/V1073. Adds a player profile and wallet UI layer without removing the existing V1073 systems.

Important: real-money ZIVO purchasing is intentionally NOT activated in V1081. It requires a server-verified payment provider/webhook before any real currency is accepted or ZIVO is credited.

Deploy: firebase deploy --only hosting
